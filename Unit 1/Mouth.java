package unit_1_introOOP;

import java.awt.Color;
import java.awt.Graphics;

public class Mouth {
	public void mouth(Graphics g, int x, int y, int len, int wid, double scale) {
		// Variables
		int w = (int) (wid * scale);
		int l = (int) (len * scale);
		
		int lineX = x;
		int lineX1 = x;
		
		
	}
}
